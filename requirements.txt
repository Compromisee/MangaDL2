# Everything needed to run MangaDL with all interfaces.
#
# Prefer the package extras if you are installing from source:
#     pip install -e ".[all]"      GUI + TUI + tray
#     pip install -e ".[gui]"      desktop app only
#     pip install -e ".[tray]"     background/system-tray mode
#
# This file is the "give me everything" list.

# --- core -------------------------------------------------------------
requests>=2.31
beautifulsoup4>=4.12
rich>=13.0
Pillow>=10.0
fpdf2>=2.7
EbookLib>=0.18

# --- desktop app ------------------------------------------------------
pywebview>=5.0

# --- full-screen terminal UI -----------------------------------------
textual>=0.60

# --- system tray / background mode -----------------------------------
# Optional: without it "Minimise to system tray" is unavailable and the
# window closes normally. Importing pystray needs a desktop session --
# on a headless box it raises at import time, which MangaDL handles.
pystray>=0.19

# --- phone / LAN server (server.py) ---------------------------------
# Optional: without it server.py explains how to install it and exits.
flask>=3.0
