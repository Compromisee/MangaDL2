[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mangadl"
version = "1.1.0"
description = "Download manga from MangaDex, Mangakatana, Natomanga and Weeb Central as CBZ, PDF or EPUB - CLI, GUI and TUI"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31",
    "beautifulsoup4>=4.12",
    "rich>=13.0",
    "Pillow>=10.0",
    "fpdf2>=2.7",
    "EbookLib>=0.18",
]

[project.optional-dependencies]
gui = ["pywebview>=5.0"]
tui = ["textual>=0.60"]
dev = ["pytest>=7.0"]
all = ["pywebview>=5.0", "textual>=0.60"]

[project.urls]
Homepage = "https://github.com/Compromisee/WeebDL"
Issues = "https://github.com/Compromisee/WeebDL/issues"

[project.scripts]
mangadl = "mangadl.cli:main"
mangadl-gui = "mangadl.gui:run_gui"
mangadl-tui = "mangadl.tui:run_tui"

[tool.setuptools.packages.find]
include = ["mangadl*"]

[tool.setuptools.package-data]
"mangadl.gui" = ["web/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["network: hits live sites; needs MANGADL_NETWORK_TESTS=1"]
